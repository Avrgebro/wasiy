# Feature-First React Structure

Wasiy will organize the Vite React SPA by product feature, with route files composing feature modules. Features own their API wrappers, query hooks, schemas, pages, and components, while shared layout, UI primitives, auth setup, API client behavior, and utilities live in central app, component, and lib directories.
