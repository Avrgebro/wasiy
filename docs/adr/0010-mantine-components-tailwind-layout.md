# Mantine Components With Tailwind Layout

Wasiy will use Mantine for interactive UI components such as inputs, selects, modals, drawers, menus, tabs, notifications, dates, and tables-adjacent controls, while using semantic HTML and Tailwind CSS classes for page and component layout. Mantine layout primitives such as `Grid`, `Box`, and `Stack` should be avoided so the app has a clear styling boundary.
