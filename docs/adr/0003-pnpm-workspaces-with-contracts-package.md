# pnpm Workspaces With Contracts Package

Wasiy will use pnpm workspaces for JavaScript and TypeScript workspace management, with Composer kept inside the Laravel API application. The initial shared package will be limited to API contracts and frontend-consumable schemas/types so the monorepo avoids becoming a dumping ground for premature shared code.
