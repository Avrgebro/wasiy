# Laravel Conventions With Workflow Actions

Wasiy will keep the Laravel API close to standard Laravel conventions while using action classes for meaningful workflow operations such as visitor check-in, resident invitation, CSV import, and reservation approval. The project will avoid repository abstractions and heavy module frameworks in v1 unless concrete complexity requires them.
