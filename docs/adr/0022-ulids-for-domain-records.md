# ULIDs for Domain Records

Wasiy will use ULIDs for primary keys on product and domain records. ULIDs are safe to expose in URLs and API responses, avoid leaking sequential record counts, can be generated application-side, and remain roughly sortable by creation time.
