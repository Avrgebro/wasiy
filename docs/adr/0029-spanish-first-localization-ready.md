# Spanish-First Localization-Ready Implementation

Wasiy will launch v1 with Spanish as the primary product language while implementing the frontend, backend messages, validation copy, and email templates with localization in mind. The React SPA will use i18next/react-i18next with Spanish as the default locale so English can be added later without rewriting component logic or hard-coded user-facing strings.
