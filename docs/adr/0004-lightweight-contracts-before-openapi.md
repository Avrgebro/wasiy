# Lightweight Contracts Before OpenAPI

Wasiy will start with lightweight TypeScript contracts and typed frontend API client wrappers instead of requiring OpenAPI from day one. OpenAPI remains the preferred later contract strategy once the API stabilizes or when mobile/external clients make generated contracts more valuable than the setup cost.
