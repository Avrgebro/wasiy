# Sail Local Development Stack

Wasiy will standardize local backend development on Laravel Sail with PostgreSQL, Redis, Mailpit, and MinIO. The Vite React frontend will run separately with pnpm so the local environment mirrors the production dependencies needed for queues, email testing, and S3-compatible amenity photo uploads.
