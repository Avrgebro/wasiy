# Monorepo With Separate API and Web Apps

Wasiy will use a monorepo with separate `apps/api` and `apps/web` applications. This keeps the Laravel API and Vite React SPA independently buildable and deployable while avoiding the coordination overhead of multiple repositories during early product development.
