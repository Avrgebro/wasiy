# React Hook Form and Zod for Frontend Forms

Wasiy will use React Hook Form and Zod for frontend forms and client-side validation in the Vite React SPA. Laravel request validation remains authoritative on the server, while Zod schemas provide fast client feedback, TypeScript inference, and reusable frontend-facing contract definitions.
